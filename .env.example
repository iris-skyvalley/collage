# Supabase project for the shared library. Leave unset to keep the library in
# the browser (IndexedDB). Apply supabase/migrations first and enable
# anonymous sign-in under Authentication → Providers.
VITE_SUPABASE_URL=https://yphbcunwlzlydoepporn.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
